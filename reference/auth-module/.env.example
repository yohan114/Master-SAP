# Copy to .env (never commit real secrets). In production, load these from a vault / OS secret store.
NODE_ENV=production          # makes session cookies Secure (HTTPS-only)
PORT=4100
DB_FILE=./auth.db            # in production point at PostgreSQL via your data layer, not SQLite
# SESSION_SECRET / DB creds etc. belong here in a real deployment.
