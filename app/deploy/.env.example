# UMMS platform — production env. Copy to app/deploy/.env and set a real DB password.
# Never commit the real .env (repo .gitignore blocks it).

# PostgreSQL password for the umms DB user (used by both the db and app containers).
DB_PASSWORD=change-me-to-a-long-random-string
